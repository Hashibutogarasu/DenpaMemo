{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AbnormalityTypes",
  "description": "Abnormality-status master data, parsed into AbnormalityType (lib/domain/master_data/abnormality_type.dart).",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id", "displayName"],
    "properties": {
      "id": {
        "type": "string",
        "description": "Stable identifier referenced by other data and code."
      },
      "displayName": {
        "type": "string",
        "description": "Name shown to the user."
      }
    }
  }
}
