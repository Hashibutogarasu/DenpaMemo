{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Attributes",
  "description": "Attribute master data, parsed into Attribute (lib/domain/master_data/attribute.dart).",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id", "index"],
    "properties": {
      "id": {
        "type": "string",
        "description": "Stable identifier referenced by other data and code, and looked up in the app's localization resources for display text."
      },
      "index": {
        "type": "integer",
        "minimum": 0,
        "description": "Display order in the UI, independent of the entry's position in this file."
      }
    }
  }
}
