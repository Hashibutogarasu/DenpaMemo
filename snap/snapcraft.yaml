name: denpa_memo
version: 1.0.0
summary: Generated by Icons Launcher
description: Generated by Icons Launcher package for Linux.

confinement: strict
base: core22
grade: stable

slots:
  dbus-denpa_memo: # adjust accordingly to your app name
    interface: dbus
    bus: session
    name: org.bar.denpa_memo # adjust accordingly to your app name

apps:
  denpa_memo:
    command: denpa_memo
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
    - network
    slots:
      - dbus-denpa_memo
parts:
  denpa_memo:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart # The main entry-point file of the application
