name: denpa_widgets
description: Graph + DataPack を合成した、denpa_memoアプリの再利用可能なFlutterウィジット一式。
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
