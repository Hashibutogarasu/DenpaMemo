name: graphql_client
description: Client for server module
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  data_pack:
    path: ../DataPack
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  graphql_flutter: ^5.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
