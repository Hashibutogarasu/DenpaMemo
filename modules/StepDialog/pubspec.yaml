name: step_dialog
description: Utility library of step runner
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  slang_flutter: ^4.19.0
  toaster:
    path: ../Toaster

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  slang: ^4.19.0
  build_runner: ">=2.15.1 <2.16.0"

flutter:
