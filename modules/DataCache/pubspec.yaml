name: data_cache
description: Offline data cache backed by SQLite, storing input/output pairs behind a hash index.
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  app_datas:
    path: ../AppDatas
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  flutter_riverpod: ">=3.3.0 <3.4.0"
  path: ^1.9.1
  sqlite3: ^3.5.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
