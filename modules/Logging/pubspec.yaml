name: app_logging
description: App-wide debug logging domain - log entry models, log stores, and interceptors for the debug log screen.
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cuid2: ^4.1.0
  flutter_riverpod: ">=3.3.0 <3.4.0"
  freezed_annotation: ^3.1.0
  graphql_flutter: ^5.3.0
  http: ^1.6.0
  json_annotation: ^4.12.0
  logger: ^2.5.0
  path: ^1.9.1

dev_dependencies:
  build_runner: ">=2.15.1 <2.16.0"
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.2.5
  json_serializable: ^6.14.1

flutter:
