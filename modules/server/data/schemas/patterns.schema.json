{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Patterns",
  "description": "Pattern-rank master data, parsed into Pattern (lib/domain/master_data/pattern.dart).",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id"],
    "properties": {
      "id": {
        "type": "string",
        "description": "Stable identifier referenced by other data and code, and looked up in the app's localization resources for display text."
      }
    }
  }
}
