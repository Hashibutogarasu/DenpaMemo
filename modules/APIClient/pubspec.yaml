name: api_client
description: REST client for modules/server's generic /tables endpoints.
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  http: ^1.6.0
  json_annotation: ^4.12.0
  app_logging:
    path: ../Logging

dev_dependencies:
  build_runner: ">=2.15.1 <2.16.0"
  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  json_serializable: ^6.14.1
  test: ^1.25.6

flutter:
