name: app_metadata
description: Provides the @AppMetaData annotation and its source_gen builder.
version: 0.1.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  analyzer: ^10.2.0
  build: ^4.0.0
  source_gen: ^4.2.4
  yaml: ^3.1.4

dev_dependencies:
  build_runner: ^2.15.1
  lints: ^6.0.0
  test: ^1.25.0
